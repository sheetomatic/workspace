import "@/components/saas/leads-machine.css";

export default function DispatchPublicLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div className="dispatch-public-layout">{children}</div>;
}
