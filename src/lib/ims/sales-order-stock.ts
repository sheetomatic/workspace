/** IMS is the runtime for sales-order stock verification (replaces a separate stock module). */
export const IMS_SALES_ORDER_STOCK_PATH = "/app/ims/orders";

export const IMS_STOCK_FULFILLMENT_STATUSES = ["CONFIRMED", "STOCK_CHECK"] as const;
