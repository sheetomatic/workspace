# Copy to .env.shared.local on this machine (gitignored).
# Vercel will not decrypt Sensitive vars for the CLI. This file is the
# readable source for shared platform keys when onboarding a new portal.
# Never commit .env.shared.local. Never paste values into chat.

OPENAI_API_KEY=""
DATABASE_URL=""
DIRECT_URL=""
RESEND_API_KEY=""
CRON_SECRET=""
